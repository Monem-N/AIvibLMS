/**
 * NotFound Page
 * 
 * Page wrapper for the NotFound component.
 */

import React from 'react';
import NotFoundComponent from '../components/errors/NotFound';

const NotFound: React.FC = () => {
  return <NotFoundComponent />;
};

export default NotFound;
